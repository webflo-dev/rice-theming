#!/usr/bin/env bash

function get_brightness(){
    light -G
}

case $1 in
    up)
        light -A 5%
        get_brightness
    ;;
    down)
        light -U 5%
        get_brightness
    ;;
    *)
        get_brightness
    ;;
esac


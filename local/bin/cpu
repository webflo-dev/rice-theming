#!/usr/bin/env bash

get_load(){
    grep -o "^[^ ]*" /proc/loadavg
}

case $1 in
    --value)
        value=$(get_load)
        [[ $2 == "--percentage" ]] && echo "$value%" || echo "$value"
    ;;
    *)
        echo "usage: cpu [OPTIONS]
		
OPTIONS
   --value [--percentage]
"
    ;;
esac